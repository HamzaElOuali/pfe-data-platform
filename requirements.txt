# ══════════════════════════════════════════════════════════════
# PFE Data Platform — Dépendances Airflow (Phase A)
# Packages compatibles avec apache-airflow==2.9.1 + numpy 1.26.x
# Les packages ML (numpy 2.x) sont installés séparément dans le Dockerfile
# ══════════════════════════════════════════════════════════════

# ─── Core Data ────────────────────────────────────────────────
pandas==2.2.2
duckdb==0.10.3
pyarrow==14.0.2
python-dotenv==1.0.1

# ─── Ingestion (Apache Beam — DirectRunner) ──────────────────
# >=2.56.0 supprime la contrainte numpy<1.25 de la 2.54.0
apache-beam>=2.56.0,<2.59.0
requests==2.31.0
psycopg2-binary==2.9.9
sqlalchemy>=1.4.36,<2.0

# ─── Transformation (dbt + DuckDB) ──────────────────────────
dbt-core==1.7.14
dbt-duckdb==1.7.3

# ─── Orchestration ──────────────────────────────────────────
apache-airflow==2.9.1

# ─── HTTP async client (AI recommender — OpenRouter) ────────
httpx>=0.27.0

# ─── Storage Client ─────────────────────────────────────────
minio==7.2.5
